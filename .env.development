GENERATE_SOURCEMAP=false

# El backend ocupa el 3000, asi que CRA arranca en el 3001.
# REACT_APP_API_URL se deja sin definir a proposito: axios.ts cae por defecto
# en http://localhost:3000, que es justo el backend local.
PORT=3001
